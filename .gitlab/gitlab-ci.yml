stages:
  - test
  - deploy

test:
  stage: test
  image: python:3.13
  script:
    - pip install uv
    - uv sync --frozen --no-cache
    - pytest --cov=src
  only:
    - main
    - develop

deploy:
  stage: deploy
  script:
    - echo "Deploying to production..."
  only:
    - main